import SwiftUI

extension Color {
    // Sky gradient colors
    static let skyTop = Color(red: 0.53, green: 0.81, blue: 0.92)
    static let skyMiddle = Color(red: 0.68, green: 0.85, blue: 0.90)
    static let skyBottom = Color(red: 0.85, green: 0.93, blue: 0.98)
    static let horizonGlow = Color(red: 1.0, green: 0.98, blue: 0.95)

    // Dreamy accent colors
    static let dreamyPurple = Color(red: 0.70, green: 0.55, blue: 0.85)
    static let dreamyMint = Color(red: 0.60, green: 0.90, blue: 0.85)
    static let dreamyPeach = Color(red: 1.0, green: 0.85, blue: 0.75)

    // Theme colors
    static let calmBlue = Color(red: 0.45, green: 0.65, blue: 0.85)
    static let calmBlueLight = Color(red: 0.60, green: 0.78, blue: 0.95)

    // Text colors
    static let softText = Color(red: 0.20, green: 0.25, blue: 0.35)
    static let lightText = Color(red: 0.50, green: 0.55, blue: 0.65)
}
